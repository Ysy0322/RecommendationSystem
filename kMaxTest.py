import numpy as np
import heapq
import util
import user_based as UB

# K = 4
# a = np.array([0.1, 0, 0.8, 0, 0.4, 0.5, 0.8, 8, 0, 0.4, 2])
# print(a)
# b = 3 * [0.0]
#
# b[0] = a[np.argpartition(a, -K)[-K:]]
# print(b[0])
#
# c = heapq.nlargest(4, range(len(a)), a.take)
# print(c)

